"""Shared package constants."""

REPOCONF_NAME = "repoconf"
MANAGED_FILE_NAME = f"{REPOCONF_NAME}.config"
INCLUDE_PATH = f"../{MANAGED_FILE_NAME}"
CONFIG_BRANCH = "__repoconf/default/main"
BACKEND_DIR_NAME = "repoconf_backend"
